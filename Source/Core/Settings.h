#pragma once

namespace Settings {
    const char version[] = "version";
    const char window[] = "window";
    const char width[] = "width";
    const char height[] = "height";

    void init();
    void flush();
}
